export default function Header() {
    return (
        <header className="header main" >
            <h1>Replicatedit</h1>
            <input type="search" />
            <button>Login</button>
            <button>ø</button>
        </header>
    )
}