export default function Header() {
    return (
        <header className="header" >
            <h1>Replicatedit</h1>
            <input type="search" />
            <button>Login</button>
            <button>ø</button>
        </header>
    )
}